# Docker Compose Environment Variables
# Copy this to .env.docker and fill in your values

# GitHub OAuth App credentials (create at https://github.com/settings/developers)
# Callback URL should be: http://YOUR_DOMAIN/auth/callback
GITHUB_CLIENT_ID=your_client_id_here
GITHUB_CLIENT_SECRET=your_client_secret_here

# JWT secret for signing tokens (generate a random secure string)
# You can generate one with: openssl rand -hex 32
JWT_SECRET=your_jwt_secret_key_change_me_make_it_long_and_random

# Comma-separated list of allowed GitHub usernames
ALLOWED_USERS=user1,user2,user3

# Frontend URL (for CORS and OAuth redirects)
# For local: http://localhost
# For production: https://yourdomain.com
FRONTEND_URL=http://localhost

# Optional: API URL for frontend (leave empty to use relative URLs)
# For local: leave empty or http://localhost:8000
# For production: https://api.yourdomain.com
VITE_API_URL=
